# fly.dev.toml app configuration for development deployment
# Usage: flyctl deploy -c fly.dev.toml

app = "manibot-dev"
primary_region = "ams"

[build]

[vm]
	cpu_kind = "shared"
	cpus = 1
	memory = "512mb"
